{
  "name": "mon-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "dev": "json-server --watch db.json --port 6001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}


